#!/usr/bin/env bash
# This script displays the PID and process name of processes containing the word "bash" using pgrep.

# Use pgrep to find processes with the name "bash"
# The -l option shows the process name along with the PID
pgrep -l "bash"

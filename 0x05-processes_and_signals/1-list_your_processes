#!/usr/bin/env bash
# This script displays a list of currently running processes.

# Use the 'ps' command with options to show all processes in a user-oriented format
# with process hierarchy (tree view) and limit the output to the first 50 lines using 'head'
ps axf | head -50
